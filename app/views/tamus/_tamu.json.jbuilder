json.extract! tamu, :id, :published_at, :tglaktifitas, :kodebooking, :namatamu, :qty, :hotel, :negara, :telp, :email, :driver, :note, :paket_id, :created_at, :updated_at
json.url tamu_url(tamu, format: :json)
